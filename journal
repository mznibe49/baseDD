// a jouter : si un projet est finis, on le supp de la table project et on l'ajoute dans la table archive
// contraint ajouter ( nom matiere unique ) on veut pas avoir deux mm matiere
// contraint ajouter ( par defaut cagnote , reserve , somme gagnee sont a 0) ca nous evitera de mettre des triggers en plus ( a testé)
// contraint l attribut en attente est initialisé a faulse

// ajout trigger compte rendu tout les 25% de temps (avec un raise) --> fait
// modification de distribution apres 75% (faire une annonce avec un raise) --> pas fait
// interdire une deuxieme inscription si l'attente d'un pere est true --> fait

// faudra rajouter des triggers jpense quand on fait des insert verifier que le id_parent est bien un parent
